# Copy to a local .env for reference. Load variables in your shell; CLI does not
# execute or source this file. Never commit real credentials.
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
